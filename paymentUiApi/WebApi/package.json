{
  "dependencies": {
    "json-server": "^0.12.1"
  },
  "devDependencies": {
  },
  "scripts": {
    "start": "npm run lite",
    "lite": "lite-server",
    "json": "json-server --p 5000 --watch db.js --routes routes.json"
  }
}
